import Layout from '../components/layout';
import Header from '../components/header';

const Teams = () => {
  return (
    <Layout isHeader isNavBar Header={<Header text="Team" />}>
      teams
    </Layout>
  );
};

export default Teams;
